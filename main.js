import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'
import { Router } from "react-router";

const router = create Router [
	// Homepage
	{ path:"/" 
	element: <p>text</p>
	element: <h5>Add to Cart</h5>
	},

	path: "/about"
	element: <h2>About us</h2>
	element: <p>It is food products.</p>
	element: <p>It is a long established.</p>
	element: <h1>Com receita de destaque número {count}!</h1>
	}

	path: "/ProductGrid"
	// img and Footer router react
	element: <h6></h6>
	]);

	console.log(routerteste);

	ReactDOM.createRoot (documentgetById("root")).render {

	//Router Provider router = {router code}


